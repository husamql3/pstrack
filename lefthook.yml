pre-commit:
  commands:
    format:
      glob: "*.{js,jsx,ts,tsx,json,jsonc,css}"
      run: bunx biome format --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    build:
      run: bun run build
