/**
 * Runtime stub for Node/CLI environments (e.g. better-auth generate).
 * In Cloudflare Workers, bindings are injected by the runtime.
 * Types are provided by worker-configuration.d.ts.
 */
export {};
