{
	"name": "@pstrack/server",
	"exports": {
		"./auth": "./src/lib/auth.ts"
	},
	"scripts": {
		"predev": "../../scripts/copy-env.sh",
		"dev": "wrangler dev",
		"dev:remote": "wrangler dev --remote",
		"build": "tsdown",
		"deploy": "bun run predev && wrangler deploy --minify",
		"compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
		"start": "bun run dist/index.js",
		"types": "bun run predev && wrangler types",
		"auth:generate": "bunx @better-auth/cli@latest generate --config ./src/lib/auth.ts --output ../../packages/db/src/schema/auth.ts"
	},
	"dependencies": {
		"@pstrack/db": "workspace:*",
		"@pstrack/shared": "workspace:*",
		"@sentry/cloudflare": "^10.38.0",
		"better-auth": "^1.4.18",
		"hono": "^4.11.7"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "catalog:",
		"@types/bun": "catalog:",
		"tsdown": "^0.20.1"
	}
}
